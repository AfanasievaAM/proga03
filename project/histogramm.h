#ifndef HISTOGRAMM_H_INCLUDED
#define HISTOGRAMM_H_INCLUDED

#include <vector>
using namespace std;

void find_minmax(const vector<double>& numbers, double& min, double& max);

#endif // HISTOGRAMM_H_INCLUDED
